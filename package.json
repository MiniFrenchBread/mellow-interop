{
  "name": "mellow-interop",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "repository": "https://github.com/mellow-finance/mellow-interop",
  "scripts": {
    "compile": "forge build",
    "prettier": "forge fmt && forge fmt ./test/**",
    "test": "forge test -vvv --fork-url $(grep HOLESKY_RPC .env | cut -d '=' -f2,3,4,5) --fork-block-number 2560000",
    "coverage:report": "forge coverage -vvv --fork-url $(grep HOLESKY_RPC .env | cut -d '=' -f2,3,4,5) --fork-block-number 2560000 --report lcov && genhtml lcov.info -o report --branch-coverage && rm lcov.info",
    "sizes": "forge build --sizes --force",
    "scc:report": "scc src --sort names --no-cocomo --exclude-dir interfaces --by-file --format wide > scc-report.txt"
  }
}